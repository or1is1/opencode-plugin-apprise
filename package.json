{
  "name": "opencode-apprise-notify",
  "version": "1.0.0",
  "description": "OpenCode plugin that sends rich notifications via Apprise CLI when the agent needs your attention",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format esm",
    "test": "bun test",
    "prepublishOnly": "bun run build"
  },
  "keywords": ["opencode", "plugin", "apprise", "notification", "slack", "discord", "telegram", "attention"],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/USERNAME/opencode-apprise-notify.git"
  },
  "homepage": "https://github.com/USERNAME/opencode-apprise-notify#readme",
  "bugs": {
    "url": "https://github.com/USERNAME/opencode-apprise-notify/issues"
  },
  "engines": {
    "node": ">=18"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.2.15",
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
