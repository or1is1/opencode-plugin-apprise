{
  "name": "opencode-apprise-notify",
  "version": "1.0.0",
  "description": "OpenCode plugin that sends rich notifications via Apprise CLI when the agent needs your attention",
  "type": "module",
  "main": "dist/opencode-apprise-notify.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/opencode-apprise-notify.js",
      "default": "./dist/opencode-apprise-notify.js"
    }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/opencode-apprise-notify.js --target node --format esm && tsc --emitDeclarationOnly",
    "test": "bun test",
    "prepublishOnly": "bun run build"
  },
  "keywords": ["opencode", "plugin", "apprise", "notification", "slack", "discord", "telegram", "attention"],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/or1is1/opencode-apprise-notify.git"
  },
  "homepage": "https://github.com/or1is1/opencode-apprise-notify#readme",
  "bugs": {
    "url": "https://github.com/or1is1/opencode-apprise-notify/issues"
  },
  "engines": {
    "node": ">=18"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.2.15",
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  }
}
